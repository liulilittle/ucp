/** @file ucp_configuration.cpp
 *  @brief Translation unit for UcpConfiguration — mirrors C# Ucp.UcpConfiguration methods.
 *
 *  Most configuration members are inline-defined in the header.  This
 *  file is a placeholder required by the CMakeLists.txt build system
 *  to produce a .obj for each header-only class that has out-of-line
 *  method bodies compiled elsewhere.
 */

#include "ucp/ucp_configuration.h"
namespace ucp {
// Configuration is fully header-only; this file provides the translation unit
// for the CMakeLists.txt entry.
}
